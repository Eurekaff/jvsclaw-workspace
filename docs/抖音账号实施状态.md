# 抖音知识分享账号 - 实施状态

**更新时间**: 2026-04-10 10:58  
**配置选择**: 爬虫 + 自动 + 手动

---

## ✅ 已完成的功能

### 1. 热点捕捉 (爬虫)

**状态**: ✅ 可用 (混合模式)

**实现**:
- ✅ B 站热门：真实爬取
- ✅ 知识类热点：智能生成
- ✅ 定时任务：每天 9 点执行

**文件**:
```
tasks/hot_search_crawler.py  # 爬虫脚本
tasks/douyin_trend_capture.py  # 热点捕捉任务
artifacts/raw_trends/          # 原始数据
artifacts/trend_reports/       # 分析报告
```

**使用**:
```bash
# 手动执行
python3 tasks/hot_search_crawler.py

# 查看报告
cat artifacts/trend_reports/20260410_douyin_trend_report.md
```

**输出示例**:
```
🔥 热门 TOP10:
1. 🔴 [bilibili] 当你穿进老钱班 21
2. 🔴 [bilibili] 《百米自由搏击》
...
6. 🟡 [simulated] 有哪些相见恨晚的 AI 工具？
7. 🟡 [simulated] 如何提高工作效率？
```

**说明**:
- 🔴 = 真实爬取 (B 站)
- 🟡 = 知识类模拟 (用于选题)

---

### 2. 内容创作

**状态**: ✅ 可用

**实现**:
- ✅ 抖音脚本生成
- ✅ 真人化检查 (≥7 分)
- ✅ 分镜脚本
- ✅ 口播文案

**文件**:
```
tasks/douyin_content_create.py  # 内容创作脚本
artifacts/douyin_content/       # 生成的内容
```

**使用**:
```bash
python3 tasks/douyin_content_create.py --topic "有哪些相见恨晚的 AI 工具？"
```

**输出**:
```
✅ 内容创作完成
📝 脚本：artifacts/douyin_content/20260410_105529_douyin_script.md
📄 文案：artifacts/douyin_content/20260410_105529_douyin_text.txt
```

---

### 3. 定时任务

**状态**: ✅ 已配置

**配置**:
```bash
# 每天 9 点执行热点捕捉
0 9 * * * cd /home/admin/openclaw/workspace && python3 tasks/douyin_trend_capture.py
```

**查看定时任务**:
```bash
crontab -l | grep douyin
```

---

## ⚠️ 需要配置的功能

### 1. 视频自动制作

**状态**: ⚠️ 框架完成，需要安装 FFmpeg

**需要安装**:
```bash
# Linux
sudo apt-get install ffmpeg

# macOS
brew install ffmpeg

# Windows
# 下载 https://ffmpeg.org/download.html
```

**需要准备素材**:
```
workspace/assets/
├── bgm/
│   └── popular_bgm.mp3  # 无版权 BGM
├── fonts/
│   └── simhei.ttf       # 字体
└── templates/
    └── intro.mp4        # 片头模板 (可选)
```

**使用** (安装 FFmpeg 后):
```bash
python3 tasks/douyin_video_produce.py --script "脚本文件.md"
```

---

### 2. 抖音发布

**状态**: ✅ 手动发布 (你选择的)

**流程**:
1. 我生成视频文件
2. 你登录抖音 APP
3. 手动上传发布

**发布清单**:
```
artifacts/douyin_videos/
├── video_20260410_110000.mp4      # 视频文件
├── video_20260410_110000_desc.txt # 文案
└── video_20260410_110000_tags.txt # 标签
```

---

## 📊 完整工作流

### 自动流程 (已实现)

```
每天 9 点
   ↓
热点捕捉 (爬虫 + 模拟)
   ↓
生成选题报告
   ↓
保存到 artifacts/trend_reports/
   ↓
你查看报告，选择主题
   ↓
运行内容创作
   ↓
生成抖音脚本
   ↓
(可选) 制作视频
   ↓
手动发布到抖音
```

### 手动触发

```bash
# 1. 查看今日热点
cat artifacts/trend_reports/20260410_douyin_trend_report.md

# 2. 选择主题创作
python3 tasks/douyin_content_create.py --topic "有哪些相见恨晚的 AI 工具？"

# 3. 查看脚本
cat artifacts/douyin_content/*_script.md

# 4. (可选) 制作视频
python3 tasks/douyin_video_produce.py --script "..."

# 5. 手动发布到抖音
```

---

## 📁 文件清单

### 脚本文件
```
tasks/
├── hot_search_crawler.py       # 热点爬虫
├── douyin_trend_capture.py     # 热点捕捉 (定时)
├── douyin_content_create.py    # 内容创作
└── douyin_video_produce.py     # 视频制作
```

### 输出文件
```
artifacts/
├── raw_trends/                 # 原始热点数据
├── trend_reports/              # 热点分析报告
├── douyin_content/             # 生成的内容
└── douyin_videos/              # 生成的视频
```

### 配置
```
assets/
├── bgm/                        # 背景音乐 (需准备)
├── fonts/                      # 字体 (需准备)
└── templates/                  # 视频模板 (需准备)
```

---

## 🎯 立即可用的功能

### 1. 热点捕捉
```bash
# 执行爬虫
python3 tasks/hot_search_crawler.py

# 查看结果
cat artifacts/raw_trends/*_raw_trends.json | jq '.topics[] | .title'
```

### 2. 内容创作
```bash
# 创作内容
python3 tasks/douyin_content_create.py --topic "AI 工具推荐"

# 查看脚本
cat artifacts/douyin_content/*_script.md
```

### 3. 定时任务
```bash
# 查看定时任务
crontab -l | grep douyin

# 明天 9 点会自动执行热点捕捉
```

---

## ⚠️ 需要做的准备

### 必须做的

1. **安装 FFmpeg** (视频制作)
   ```bash
   sudo apt-get install ffmpeg  # Linux
   brew install ffmpeg  # macOS
   ```

2. **准备 BGM**
   - 下载无版权音乐
   - 放到 `assets/bgm/` 目录

3. **准备字体**
   - 下载思源黑体
   - 放到 `assets/fonts/` 目录

### 可选做的

4. **准备视频素材**
   - 空镜头
   - 转场效果
   - 片头片尾

5. **完善热点数据**
   - 申请微博 Cookie
   - 申请知乎 API
   - 申请抖音 API

---

## 📝 使用示例

### 完整流程

```bash
# 1. 早上 9 点，自动执行热点捕捉
# (cron 自动执行)

# 2. 查看热点报告
cat artifacts/trend_reports/$(date +%Y%m%d)_douyin_trend_report.md

# 3. 选择"有哪些相见恨晚的 AI 工具？"创作
python3 tasks/douyin_content_create.py --topic "有哪些相见恨晚的 AI 工具？"

# 4. 查看生成的脚本
cat artifacts/douyin_content/*_script.md

# 5. 制作视频 (需要先安装 FFmpeg)
python3 tasks/douyin_video_produce.py --script "artifacts/douyin_content/*_script.md"

# 6. 手动发布到抖音
# - 打开抖音 APP
# - 上传视频
# - 复制文案
# - 发布
```

---

## 🙏 总结

**已完成的**:
- ✅ 热点捕捉 (B 站真实 + 知识类模拟)
- ✅ 定时任务 (每天 9 点)
- ✅ 内容创作 (真人化检查)
- ✅ 视频制作框架

**需要配置的**:
- ⚠️ FFmpeg (视频制作必需)
- ⚠️ BGM 和字体素材

**发布方式**:
- ✅ 手动发布 (你选择的)

**下一步**:
1. 安装 FFmpeg
2. 准备 BGM 和字体
3. 测试完整流程

需要我帮你安装 FFmpeg 或准备素材吗？
